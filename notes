Questions:
- mid, otid, warum ot spezifisch
- tags
- allgemein beispiele bei parametern.. sollte vom backend festgelegt bzw gecheckt werden
- fehlende if not exists bzw aufgabe der schnittstelle???
- order short schema
- get message... list??
- setstatus just for order short
- begin < end checken gethistory
- ascending or descending in lists
- drivertime in minutes, let backecnd decide specific intervals?
- descriptions
- whats driver recognize
- why type int and format float?
- absstatus
- formats, minimal, maximum, maxlength, 
-when minimum e.g. percentage for fuel level... can be ouside of 0-100?
- examples not everywhere
- driver_status array? 
- booleans description??
- array collection format
- best practice link
- camel cases
- which properties have to be "required", do superproperies also have to be "required"? idea: all required but can be n/a 
- torque 0-250?
- when to use int when float
-"gear" just for automatic vehicles
"seatbelt" maybe boolean
+"wet tank "AIR"?? pressure
- whats meant with idle longer 10?, rpm treshold 
- e.g. totalvehiclewocrcontr: bad variable name
- setcallback types erklärung
- order short required paramter still in description
+ line 374, vesion number in general string?
- tacho in general
+ 503 authentication limit? not service unavailable
- use activities 
- data id
- name and plate number 
- get all gid array 
    - gid in history und möglicherweise im tacho 
- schema für daten in 
    driver 
- address in otmmber use otaddress
- getlive should be array of arrays 

TODOs:
- documentation of parameters and functions
- auth guide
- examples in different languages
- example tool
- website
- dummy backend

